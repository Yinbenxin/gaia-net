#include "ichannel.h"

namespace gaianet {
    void IChannel::statistics_output() const {
        // gaialog_info("taskid:{},partyid:{},to_partyid:{} get route sumarry network recv packets cnt {} and length of all packets {} bytes", get_channel_id(),
                    //  get_from_party(), get_to_party(), get_packet_recv(), get_bytes_recv());
        // gaialog_info("taskid:{},partyid:{},to_partyid:{} get route sumarry network send packets cnt {} and length of all packets {} bytes", get_channel_id(),
                    //  get_from_party(), get_to_party(), get_packet_send(), get_bytes_send());
    }
} // namespace gaianet